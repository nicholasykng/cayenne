<% if error_term.errors.any? %>
        <div id="error_explanation">
            <h2><%= pluralize(error_term.errors.count, "error")%> prohibited this account from being created:</h2>
            <ul>
            <% error_term.errors.full_messages.each do |msg| %>
                <li><%= msg %></li>
            <% end %>
            </ul>
        </div>
    <% end %>